// SANDBOX_START: template empty test script
import type { TestScript } from "../types";

const script: TestScript = {
  id: 'template',
  title: '(Template) Add your steps',
  steps: []
};

export default script;
// SANDBOX_END